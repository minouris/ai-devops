# Temporary workspace files (not committed)
.tmp/

# Environment variable files (not committed)
**/.env

# Claude data files (not committed)
.devcontainer/.claude-data

# Exclude root level .claude folder (mounted from devcontainer, not committed)
.claude
__pycache__/
